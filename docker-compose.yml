services:
  digest:
    build: .
    image: korea-event-alimi:latest
    restart: unless-stopped
    env_file: .env
    environment:
      TZ: Asia/Seoul
      SETTINGS_PATH: /data/settings.json
    ports:
      - "8080:8080"
    volumes:
      - ./logs:/var/log
      - ./data:/data
